## 1. Sanitize Current Candidate Evidence

- [x] 1.1 Replace the plausible real LSF `bhist` timeout capture with clearly synthetic identifiers and timestamps while preserving parser grammar, then run the focused LSF scheduler test.
- [x] 1.2 Replace deployment-derived runtime discovery paths and names with generalized synthetic paths while preserving all discovery assertions, then run the focused runtime configuration tests.
- [x] 1.3 Replace the ambiguous project UUID and research-associated project name with self-evidently synthetic values, then run the focused lifecycle and submission-dialog tests.
- [x] 1.4 Delete the unreferenced obsolete-brand `docs/ui_reference/future_light.png`, then verify the file is absent and no repository text references it.
- [x] 1.5 Confirm `.gitignore` no longer contains the real runtime UUID and still contains the generic runtime/cache rules required by the approved inventory.

## 2. Record Deferred Provenance Work

- [x] 2.1 Create an independent draft OpenSpec follow-up change that records Au electrode coordinate provenance/licensing, Au placement-default scientific provenance, and covalent/van-der-Waals radii provenance as public-release prerequisites without modifying those resources.

## 3. Validate the Candidate

- [x] 3.1 Run all directly affected synthetic unit tests and record the result.
- [x] 3.2 Run the full offline test suite and record collected, passed, failed, skipped, and exit status.
- [x] 3.3 Run a current-tree privacy/provenance scan covering the approved real-environment indicators, common credential/private-key patterns, suspicious paths/identifiers, and tracked resource inventory; report any residual findings without rewriting history.
- [x] 3.4 Run strict OpenSpec validation for this change and audit the final Git status/diff for scope deviations.
